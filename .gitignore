# git ls-files --others --exclude-from=.git/info/exclude
# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):
# *.[oa]
# *~
#
# MMehr, 2015/01/24 - added suggestions from http://stackoverflow.com/questions/5383609/using-git-with-an-existing-xcode-project
#  These are needed to exclude Xcode build artifacts from source code.
#  Usage (when adding an existing project to git):
#    …$ git init
#    …$ cp -f ../prototype.gitignore .git/info/exclude
#    …$ git add .
#    …$ git commit -m "Initial commit."
#
# MMehr, 2015/01/24 - added *.pch also
#
UserInterfaceState.xcuserstate
build
*.pbxuser
*.perspectivev3
*.mode1v3
*~
*~.nib
*~.xib 
.DS_Store 
xcuserdata/
*.pch
Swift/XCatalog.xcodeproj/xcuserdata
XCatalog.xccheckout
.gitignore.bak
Swift/XCatalog.xcodeproj/project.xcworkspace
